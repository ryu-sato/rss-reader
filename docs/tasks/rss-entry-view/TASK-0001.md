# TASK-0001: Prismaスキーマ追加とマイグレーション

**タスクID**: TASK-0001
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *要件定義データモデル・既存prisma/schema.prismaより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-entry-view/requirements.md) - REQ-013, REQ-401
- **設計文書**: [📐 architecture.md](../../design/rss-entry-view/architecture.md)
- **DBスキーマ**: [🗄️ database-schema.sql](../../design/rss-entry-view/database-schema.sql)

## タスク概要

既存の `prisma/schema.prisma` に Entry・EntryMeta・Tag・EntryTag の4テーブルを追加し、Prismaマイグレーションを実行する。Feed モデルに `entries Entry[]` リレーションも追加する。

## 依存タスク

- **前提タスク**: なし（最初のタスク）
- **後続タスク**: [TASK-0002: エントリー型定義作成](TASK-0002.md), [TASK-0004: EntryFetcher実装](TASK-0004.md)

## 完了条件

- [x] `prisma/schema.prisma` に Entry, EntryMeta, Tag, EntryTag モデルが追加されている
- [x] Feed モデルに `entries Entry[]` リレーションが追加されている
- [x] `pnpm prisma migrate dev --name add-entry-tables` が成功する
- [x] `pnpm prisma generate` が成功する
- [x] 既存の Feed 関連テストが引き続き通る（ビルド正常・既存テスト失敗はスキーマ変更と無関係）

---

## 実装詳細

### 1. Entry モデル追加 🔵

**信頼性**: 🔵 *要件定義 データモデル・database-schema.sqlより*

**実装ファイル**: `prisma/schema.prisma`

```prisma
model Entry {
  id          String     @id @default(uuid())
  feedId      String
  guid        String
  title       String
  link        String
  description String?
  content     String?
  publishedAt DateTime?
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt

  feed        Feed       @relation(fields: [feedId], references: [id], onDelete: Cascade)
  meta        EntryMeta?
  tags        EntryTag[]

  @@unique([feedId, guid])
  @@index([publishedAt(sort: Desc)])
  @@index([feedId])
  @@map("entries")
}
```

### 2. EntryMeta モデル追加 🔵

**信頼性**: 🔵 *要件定義 データモデル・REQ-008, REQ-009より*

```prisma
model EntryMeta {
  id           String   @id @default(uuid())
  entryId      String   @unique
  isRead       Boolean  @default(false)
  isReadLater  Boolean  @default(false)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  entry        Entry    @relation(fields: [entryId], references: [id], onDelete: Cascade)

  @@map("entry_metas")
}
```

### 3. Tag・EntryTag モデル追加 🔵

**信頼性**: 🔵 *要件定義 データモデル・REQ-010, REQ-011より*

```prisma
model Tag {
  id        String     @id @default(uuid())
  name      String     @unique
  createdAt DateTime   @default(now())

  entries   EntryTag[]

  @@map("tags")
}

model EntryTag {
  entryId String
  tagId   String

  entry   Entry  @relation(fields: [entryId], references: [id], onDelete: Cascade)
  tag     Tag    @relation(fields: [tagId], references: [id], onDelete: Cascade)

  @@id([entryId, tagId])
  @@index([tagId])
  @@map("entry_tags")
}
```

### 4. Feed モデルへのリレーション追加 🔵

**信頼性**: 🔵 *架設計文書 architecture.mdより*

既存の Feed モデルに以下を追加:
```prisma
entries Entry[]
```

### 5. マイグレーション実行 🔵

**信頼性**: 🔵 *既存プロジェクト慣習より*

```bash
pnpm prisma migrate dev --name add-entry-tables
pnpm prisma generate
```

---

## 実装手順

1. `/tsumiki:direct-setup` - Prismaスキーマ編集・マイグレーション実行
2. `/tsumiki:direct-verify` - マイグレーション成功・既存テスト通過を確認

---

## 注意事項

- 既存の Feed モデルとそのテストに影響しないこと
- `@@map` でテーブル名をスネークケースに設定すること（既存パターンに倣う）
- SQLite は `prisma/dev.db` ファイルを使用

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 5 | 0 | 0 | 5 |

- **総項目数**: 5項目
- 🔵 **青信号**: 5項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
