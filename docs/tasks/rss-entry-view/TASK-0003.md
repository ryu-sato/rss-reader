# TASK-0003: node-cronパッケージ追加と基本設定

**タスクID**: TASK-0003
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *設計文書 architecture.md・ヒアリング: node-cronより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-entry-view/requirements.md) - REQ-211
- **設計文書**: [📐 architecture.md](../../design/rss-entry-view/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/rss-entry-view/dataflow.md)

## タスク概要

`node-cron` と `@types/node-cron` を pnpm でインストールする。`src/instrumentation.ts` の雛形を作成し、Next.js Instrumentation Hook の基本構造を整える（cron実体は TASK-0008 で実装）。

## 依存タスク

- **前提タスク**: [TASK-0001: Prismaスキーマ追加](TASK-0001.md)
- **後続タスク**: [TASK-0008: CronScheduler実装](TASK-0008.md)

## 完了条件

- [ ] `node-cron` と `@types/node-cron` が `package.json` に追加されている
- [ ] `pnpm install` が成功する
- [ ] `src/instrumentation.ts` が存在し、`export async function register() {}` の雛形がある
- [ ] `next.config.js` または `next.config.ts` で `instrumentationHook: true` が設定されている（Next.js 14以下の場合）
- [ ] TypeScript コンパイルエラーなし

---

## 実装詳細

### 1. パッケージインストール 🔵

**信頼性**: 🔵 *設計文書 architecture.md 新規追加パッケージより*

```bash
pnpm add node-cron
pnpm add -D @types/node-cron
```

### 2. src/instrumentation.ts 雛形作成 🔵

**信頼性**: 🔵 *設計文書 architecture.md・Next.js 14+ Instrumentation Hook公式パターンより*

**実装ファイル**: `src/instrumentation.ts`

```typescript
export async function register() {
  if (process.env.NEXT_RUNTIME === 'nodejs') {
    // CronScheduler は TASK-0008 で実装
    // await import('./lib/cron')
  }
}
```

### 3. next.config 設定確認 🟡

**信頼性**: 🟡 *Next.js バージョンによって設定方法が異なるため妥当な推測*

Next.js 14以下の場合は `next.config.js` に以下を追加:
```javascript
experimental: {
  instrumentationHook: true,
}
```
Next.js 15以上では不要（デフォルト有効）。

---

## 実装手順

1. `/tsumiki:direct-setup` - パッケージインストール・instrumentation.ts作成
2. `/tsumiki:direct-verify` - ビルド成功・TypeScriptエラーなしを確認

---

## 注意事項

- パッケージマネージャーは **pnpm** を使用すること（npm/yarn 不可）
- instrumentation.ts は `src/` ディレクトリ直下に配置（Next.js App Router の慣習）
- `process.env.NEXT_RUNTIME === 'nodejs'` 条件を必ず入れること（Edge Runtime での実行防止）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 1 | 0 | 3 |

- **総項目数**: 3項目
- 🔵 **青信号**: 2項目 (67%)
- 🟡 **黄信号**: 1項目 (33%)

**品質評価**: 高品質
