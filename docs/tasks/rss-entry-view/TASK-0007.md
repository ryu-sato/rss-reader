# TASK-0007: TagService実装（lib/tag-service.ts）

**タスクID**: TASK-0007
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 2 - バックエンド実装
**信頼性レベル**: 🟡 *要件定義 REQ-010, REQ-011, REQ-012・設計文書より（TagService層は推測含む）*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-entry-view/requirements.md) - REQ-010, REQ-011, REQ-012
- **設計文書**: [📐 architecture.md](../../design/rss-entry-view/architecture.md)
- **API仕様**: [🔌 api-endpoints.md](../../design/rss-entry-view/api-endpoints.md)

## タスク概要

`src/lib/tag-service.ts` を実装する。タグ名の小文字正規化、タグの upsert・エントリーへの付与、タグのエントリーからの解除、タグ一覧取得を実装する。

## 依存タスク

- **前提タスク**: [TASK-0006: EntryService一覧取得](TASK-0006.md)
- **後続タスク**: [TASK-0011: API Route Handler タグ](TASK-0011.md)

## 完了条件

- [ ] `src/lib/tag-service.ts` が作成されている
- [ ] `upsertTagAndAssign(name, entryId)` でタグ作成・エントリー付与が動作する
- [ ] タグ名が小文字正規化されて保存される（"Tech" → "tech"）
- [ ] `removeTagFromEntry(tagId, entryId)` でタグ解除が動作する
- [ ] `getAllTags()` で全タグ一覧が取得できる
- [ ] 主要機能の単体テストが通る

---

## 実装詳細

### 1. upsertTagAndAssign 関数 🔵

**信頼性**: 🔵 *要件定義 REQ-010, REQ-011, REQ-012・ヒアリング: タグcase-insensitiveより*

**実装ファイル**: `src/lib/tag-service.ts`

```typescript
export async function upsertTagAndAssign(name: string, entryId: string): Promise<Tag> {
  const normalizedName = name.toLowerCase().trim()

  const tag = await prisma.tag.upsert({
    where: { name: normalizedName },
    create: { name: normalizedName },
    update: {},
  })

  await prisma.entryTag.upsert({
    where: { entryId_tagId: { entryId, tagId: tag.id } },
    create: { entryId, tagId: tag.id },
    update: {},
  })

  return tag
}
```

### 2. removeTagFromEntry 関数 🔵

**信頼性**: 🔵 *要件定義 REQ-010・API仕様 DELETE /api/tags/:tagId/entries/:entryId より*

```typescript
export async function removeTagFromEntry(tagId: string, entryId: string): Promise<void> {
  await prisma.entryTag.delete({
    where: { entryId_tagId: { entryId, tagId } },
  })
}
```

### 3. getAllTags 関数 🔵

**信頼性**: 🔵 *要件定義 REQ-012・API仕様 GET /api/tags より*

```typescript
export async function getAllTags(): Promise<Tag[]> {
  return prisma.tag.findMany({ orderBy: { name: 'asc' } })
}
```

---

## 単体テスト要件

### テストケース1: upsertTagAndAssign - 大文字小文字の正規化 🔵

**信頼性**: 🔵 *ヒアリング: タグcase-insensitiveより*

**Given**: "Tech" というタグ名でエントリーに付与
**When**: `upsertTagAndAssign("Tech", entryId)` を呼び出す
**Then**: タグが "tech" として保存される

### テストケース2: upsertTagAndAssign - 既存タグの再利用 🔵

**信頼性**: 🔵 *REQ-012・upsert設計より*

**Given**: "tech" タグが既にDBに存在する
**When**: `upsertTagAndAssign("TECH", entryId)` を呼び出す
**Then**: 新しいタグは作成されず、既存の "tech" タグがエントリーに付与される

### テストケース3: removeTagFromEntry 🔵

**信頼性**: 🔵 *REQ-010より*

**Given**: エントリーにタグが付与されている
**When**: `removeTagFromEntry(tagId, entryId)` を呼び出す
**Then**: EntryTag が削除され、Tagテーブルは変更されない

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0007` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- タグ名は必ず `toLowerCase().trim()` して保存すること
- `removeTagFromEntry` はタグ自体は削除しない（EntryTag のみ削除）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 単体テスト | 3 | 0 | 0 | 3 |

- **総項目数**: 6項目
- 🔵 **青信号**: 6項目 (100%)

**品質評価**: 高品質
