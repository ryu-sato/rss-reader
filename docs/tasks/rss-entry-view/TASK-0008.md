# TASK-0008: CronScheduler実装（lib/cron.ts + instrumentation.ts）

**タスクID**: TASK-0008
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 2 - バックエンド実装
**信頼性レベル**: 🔵 *要件定義 REQ-211・設計文書 architecture.md・ヒアリング: node-cronより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-entry-view/requirements.md) - REQ-211
- **設計文書**: [📐 architecture.md](../../design/rss-entry-view/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/rss-entry-view/dataflow.md)

## タスク概要

`src/lib/cron.ts` に node-cron のスケジュール定義を実装し、`src/instrumentation.ts` の `register()` 関数内で起動する。毎時0分（`0 * * * *`）に `fetchAllFeedsEntries()` を実行する。

## 依存タスク

- **前提タスク**: [TASK-0005: EntryService保存・重複排除](TASK-0005.md), [TASK-0003: node-cronパッケージ追加](TASK-0003.md)
- **後続タスク**: [TASK-0017: E2E統合テスト](TASK-0017.md)

## 完了条件

- [ ] `src/lib/cron.ts` が作成されている
- [ ] `node-cron.schedule('0 * * * *', ...)` で毎時0分にスケジュールされている
- [ ] `src/instrumentation.ts` の `register()` 内で `process.env.NEXT_RUNTIME === 'nodejs'` 条件付きで cron が起動する
- [ ] Next.js 開発サーバー起動時に cron が正常起動することを確認
- [ ] TypeScript コンパイルエラーなし

---

## 実装詳細

### 1. lib/cron.ts 実装 🔵

**信頼性**: 🔵 *設計文書 architecture.md CronScheduler・ヒアリング: node-cronより*

**実装ファイル**: `src/lib/cron.ts`

```typescript
import cron from 'node-cron'
import { fetchAllFeedsEntries } from './entry-service'

export function startCronScheduler(): void {
  cron.schedule('0 * * * *', async () => {
    console.log('[Cron] Starting scheduled entry fetch...')
    try {
      await fetchAllFeedsEntries()
      console.log('[Cron] Scheduled entry fetch completed.')
    } catch (error) {
      console.error('[Cron] Scheduled entry fetch failed:', error)
    }
  })
  console.log('[Cron] Scheduler started (every hour at :00)')
}
```

### 2. instrumentation.ts 更新 🔵

**信頼性**: 🔵 *設計文書 architecture.md・Next.js 14+ Instrumentation Hook公式より*

**実装ファイル**: `src/instrumentation.ts`

```typescript
export async function register() {
  if (process.env.NEXT_RUNTIME === 'nodejs') {
    const { startCronScheduler } = await import('./lib/cron')
    startCronScheduler()
  }
}
```

---

## 実装手順

1. `/tsumiki:direct-setup` - cron.ts 実装・instrumentation.ts 更新
2. `/tsumiki:direct-verify` - 開発サーバー起動・cron起動ログ確認

---

## 注意事項

- `process.env.NEXT_RUNTIME === 'nodejs'` 条件必須（Edge Runtime での実行防止）
- 動的 import (`await import('./lib/cron')`) を使用してサーバーサイドのみで読み込む
- cron の実行タイミングは `0 * * * *`（毎時0分）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |

- **総項目数**: 2項目
- 🔵 **青信号**: 2項目 (100%)

**品質評価**: 高品質
