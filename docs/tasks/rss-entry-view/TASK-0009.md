# TASK-0009: API Route Handler - エントリー一覧・詳細取得

**タスクID**: TASK-0009
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 2 - バックエンド実装
**信頼性レベル**: 🔵 *API仕様 GET /api/entries, GET /api/entries/:id より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-entry-view/requirements.md) - REQ-001, REQ-003, REQ-004, REQ-005, REQ-006
- **設計文書**: [📐 architecture.md](../../design/rss-entry-view/architecture.md)
- **API仕様**: [🔌 api-endpoints.md](../../design/rss-entry-view/api-endpoints.md)

## タスク概要

`src/app/api/entries/route.ts` と `src/app/api/entries/[id]/route.ts` を実装する。エントリー一覧取得（フィルター・ページネーション）とエントリー詳細取得のRoute Handlerを作成する。

## 依存タスク

- **前提タスク**: [TASK-0006: EntryService一覧取得](TASK-0006.md)
- **後続タスク**: [TASK-0012: エントリー一覧ページ](TASK-0012.md)

## 完了条件

- [ ] `src/app/api/entries/route.ts` に GET ハンドラーが実装されている
- [ ] `src/app/api/entries/[id]/route.ts` に GET ハンドラーが実装されている
- [ ] クエリパラメータ（feedId, tagId, page, afterId, beforeId）のバリデーションが動作する
- [ ] 存在しない ID で 404 エラーレスポンスが返る
- [ ] API仕様通りのレスポンス形式（success, data, pagination）で返る
- [ ] 主要機能の単体テストが通る

---

## 実装詳細

### 1. GET /api/entries Route Handler 🔵

**信頼性**: 🔵 *API仕様 GET /api/entries・既存 GET /api/feeds パターンより*

**実装ファイル**: `src/app/api/entries/route.ts`

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { findManyEntries } from '@/lib/entry-service'

export async function GET(request: NextRequest) {
  try {
    const { searchParams } = request.nextUrl
    const feedId = searchParams.get('feedId') ?? undefined
    const tagId = searchParams.get('tagId') ?? undefined
    const page = Number(searchParams.get('page') ?? '1')
    const limit = Number(searchParams.get('limit') ?? '20')
    const afterId = searchParams.get('afterId') ?? undefined
    const beforeId = searchParams.get('beforeId') ?? undefined

    if (isNaN(page) || page < 1) {
      return NextResponse.json(
        { success: false, error: { code: 'VALIDATION_ERROR', message: 'Invalid page parameter' } },
        { status: 400 }
      )
    }

    const result = await findManyEntries({ feedId, tagId, page, limit, afterId, beforeId })
    return NextResponse.json({ success: true, data: result.entries, pagination: result.pagination })
  } catch (error) {
    console.error('GET /api/entries error:', error)
    return NextResponse.json(
      { success: false, error: { code: 'INTERNAL_SERVER_ERROR', message: 'Internal server error' } },
      { status: 500 }
    )
  }
}
```

### 2. GET /api/entries/:id Route Handler 🔵

**信頼性**: 🔵 *API仕様 GET /api/entries/:id・既存 GET /api/feeds/:id パターンより*

**実装ファイル**: `src/app/api/entries/[id]/route.ts`

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { getEntryById } from '@/lib/entry-service'

export async function GET(request: NextRequest, { params }: { params: { id: string } }) {
  try {
    const entry = await getEntryById(params.id)
    if (!entry) {
      return NextResponse.json(
        { success: false, error: { code: 'ENTRY_NOT_FOUND', message: 'Entry not found' } },
        { status: 404 }
      )
    }
    return NextResponse.json({ success: true, data: entry })
  } catch (error) {
    console.error(`GET /api/entries/${params.id} error:`, error)
    return NextResponse.json(
      { success: false, error: { code: 'INTERNAL_SERVER_ERROR', message: 'Internal server error' } },
      { status: 500 }
    )
  }
}
```

---

## 単体テスト要件

### テストケース1: GET /api/entries - 正常系 🔵

**信頼性**: 🔵 *API仕様より*

**Given**: エントリーが存在する
**When**: `GET /api/entries?page=1` を呼び出す
**Then**: 200 レスポンスで `{ success: true, data: [...], pagination: {...} }` が返る

### テストケース2: GET /api/entries/:id - 404 🔵

**信頼性**: 🔵 *API仕様 ENTRY_NOT_FOUNDより*

**Given**: 存在しない ID
**When**: `GET /api/entries/nonexistent-id` を呼び出す
**Then**: 404 で `{ success: false, error: { code: 'ENTRY_NOT_FOUND' } }` が返る

### テストケース3: GET /api/entries - page バリデーション 🟡

**信頼性**: 🟡 *既存バリデーションパターンから妥当な推測*

**Given**: 無効な page パラメータ（例: page=-1）
**When**: `GET /api/entries?page=-1` を呼び出す
**Then**: 400 で `VALIDATION_ERROR` が返る

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0009` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- 既存の `src/app/api/feeds/route.ts` のパターンを参考にすること
- エラーメッセージは英語で記述（NFR-202）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 2 | 1 | 0 | 3 |

- **総項目数**: 5項目
- 🔵 **青信号**: 4項目 (80%)
- 🟡 **黄信号**: 1項目 (20%)

**品質評価**: 高品質
