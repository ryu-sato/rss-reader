# TASK-0010: API Route Handler - メタ情報更新

**タスクID**: TASK-0010
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - バックエンド実装
**信頼性レベル**: 🔵 *API仕様 PUT /api/entries/:id/meta より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-entry-view/requirements.md) - REQ-008, REQ-009, REQ-101
- **設計文書**: [📐 architecture.md](../../design/rss-entry-view/architecture.md)
- **API仕様**: [🔌 api-endpoints.md](../../design/rss-entry-view/api-endpoints.md)

## タスク概要

`src/app/api/entries/[id]/meta/route.ts` を実装する。エントリーの既読・後で読むフラグを更新する PUT エンドポイント。EntryMeta が存在しない場合は新規作成（upsert）する。

## 依存タスク

- **前提タスク**: [TASK-0006: EntryService一覧取得](TASK-0006.md)
- **後続タスク**: [TASK-0015: EntryModalコンポーネント](TASK-0015.md)

## 完了条件

- [ ] `src/app/api/entries/[id]/meta/route.ts` に PUT ハンドラーが実装されている
- [ ] `isRead` と `isReadLater` フラグの更新が動作する（upsert）
- [ ] 存在しない entryId で 404 エラーが返る
- [ ] API仕様通りのレスポンス形式で返る
- [ ] 主要機能の単体テストが通る

---

## 実装詳細

### 1. PUT /api/entries/:id/meta Route Handler 🔵

**信頼性**: 🔵 *API仕様 PUT /api/entries/:id/meta より*

**実装ファイル**: `src/app/api/entries/[id]/meta/route.ts`

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { getEntryById, updateEntryMeta } from '@/lib/entry-service'

export async function PUT(request: NextRequest, { params }: { params: { id: string } }) {
  try {
    const entry = await getEntryById(params.id)
    if (!entry) {
      return NextResponse.json(
        { success: false, error: { code: 'ENTRY_NOT_FOUND', message: 'Entry not found' } },
        { status: 404 }
      )
    }

    const body = await request.json()
    const { isRead, isReadLater } = body

    const data: { isRead?: boolean; isReadLater?: boolean } = {}
    if (typeof isRead === 'boolean') data.isRead = isRead
    if (typeof isReadLater === 'boolean') data.isReadLater = isReadLater

    const meta = await updateEntryMeta(params.id, data)
    return NextResponse.json({ success: true, data: meta })
  } catch (error) {
    console.error(`PUT /api/entries/${params.id}/meta error:`, error)
    return NextResponse.json(
      { success: false, error: { code: 'INTERNAL_SERVER_ERROR', message: 'Internal server error' } },
      { status: 500 }
    )
  }
}
```

---

## 単体テスト要件

### テストケース1: PUT /api/entries/:id/meta - 正常系（新規作成） 🔵

**信頼性**: 🔵 *REQ-008, REQ-101・API仕様より*

**Given**: EntryMeta が存在しないエントリー
**When**: `PUT /api/entries/:id/meta { isRead: true }` を呼び出す
**Then**: 200 で EntryMeta が新規作成されて返る（isRead: true）

### テストケース2: PUT /api/entries/:id/meta - 存在しない entryId 🔵

**信頼性**: 🔵 *API仕様 ENTRY_NOT_FOUNDより*

**Given**: 存在しない entryId
**When**: `PUT /api/entries/nonexistent/meta { isRead: true }` を呼び出す
**Then**: 404 で `ENTRY_NOT_FOUND` エラーが返る

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0010` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `isRead`/`isReadLater` は両方省略可能（Partial update）
- モーダルを開いた際の自動既読（REQ-101）はフロントエンド側（TASK-0015）から呼び出す

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1 | 0 | 0 | 1 |
| 単体テスト | 2 | 0 | 0 | 2 |

- **総項目数**: 3項目
- 🔵 **青信号**: 3項目 (100%)

**品質評価**: 高品質
