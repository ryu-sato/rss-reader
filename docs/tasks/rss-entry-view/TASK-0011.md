# TASK-0011: API Route Handler - タグCRUD

**タスクID**: TASK-0011
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - バックエンド実装
**信頼性レベル**: 🔵 *API仕様 GET/POST /api/tags, DELETE /api/tags/:tagId/entries/:entryId より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-entry-view/requirements.md) - REQ-010, REQ-011, REQ-012
- **設計文書**: [📐 architecture.md](../../design/rss-entry-view/architecture.md)
- **API仕様**: [🔌 api-endpoints.md](../../design/rss-entry-view/api-endpoints.md)

## タスク概要

タグ関連の API Route Handler を実装する。GET /api/tags（一覧）、POST /api/tags（作成・付与）、DELETE /api/tags/:tagId/entries/:entryId（解除）の3エンドポイント。

## 依存タスク

- **前提タスク**: [TASK-0007: TagService実装](TASK-0007.md)
- **後続タスク**: [TASK-0016: TagInputコンポーネント](TASK-0016.md)

## 完了条件

- [ ] `src/app/api/tags/route.ts` に GET・POST ハンドラーが実装されている
- [ ] `src/app/api/tags/[tagId]/entries/[entryId]/route.ts` に DELETE ハンドラーが実装されている
- [ ] POST でタグ名が小文字正規化されて保存される
- [ ] 存在しない entryId で 404 エラーが返る
- [ ] 主要機能の単体テストが通る

---

## 実装詳細

### 1. GET /api/tags Route Handler 🔵

**信頼性**: 🔵 *API仕様 GET /api/tagsより*

**実装ファイル**: `src/app/api/tags/route.ts`

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { getAllTags, upsertTagAndAssign } from '@/lib/tag-service'
import { getEntryById } from '@/lib/entry-service'

export async function GET() {
  try {
    const tags = await getAllTags()
    return NextResponse.json({ success: true, data: tags })
  } catch (error) {
    return NextResponse.json(
      { success: false, error: { code: 'INTERNAL_SERVER_ERROR', message: 'Internal server error' } },
      { status: 500 }
    )
  }
}

export async function POST(request: NextRequest) {
  try {
    const { name, entryId } = await request.json()

    if (!name || !entryId) {
      return NextResponse.json(
        { success: false, error: { code: 'VALIDATION_ERROR', message: 'name and entryId are required' } },
        { status: 400 }
      )
    }

    const entry = await getEntryById(entryId)
    if (!entry) {
      return NextResponse.json(
        { success: false, error: { code: 'ENTRY_NOT_FOUND', message: 'Entry not found' } },
        { status: 404 }
      )
    }

    const tag = await upsertTagAndAssign(name, entryId)
    return NextResponse.json({ success: true, data: tag }, { status: 201 })
  } catch (error) {
    return NextResponse.json(
      { success: false, error: { code: 'INTERNAL_SERVER_ERROR', message: 'Internal server error' } },
      { status: 500 }
    )
  }
}
```

### 2. DELETE /api/tags/:tagId/entries/:entryId Route Handler 🔵

**信頼性**: 🔵 *API仕様 DELETE /api/tags/:tagId/entries/:entryId より*

**実装ファイル**: `src/app/api/tags/[tagId]/entries/[entryId]/route.ts`

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { removeTagFromEntry } from '@/lib/tag-service'

export async function DELETE(
  request: NextRequest,
  { params }: { params: { tagId: string; entryId: string } }
) {
  try {
    await removeTagFromEntry(params.tagId, params.entryId)
    return NextResponse.json({ success: true })
  } catch (error) {
    return NextResponse.json(
      { success: false, error: { code: 'TAG_NOT_FOUND', message: 'Tag not found on this entry' } },
      { status: 404 }
    )
  }
}
```

---

## 単体テスト要件

### テストケース1: POST /api/tags - 正常系 🔵

**信頼性**: 🔵 *API仕様より*

**Given**: 有効な name と entryId
**When**: `POST /api/tags { name: "Tech", entryId: "xxx" }` を呼び出す
**Then**: 201 でタグが "tech"（小文字）として返る

### テストケース2: POST /api/tags - entryId が存在しない 🔵

**信頼性**: 🔵 *API仕様 ENTRY_NOT_FOUNDより*

**Given**: 存在しない entryId
**When**: `POST /api/tags { name: "tech", entryId: "nonexistent" }` を呼び出す
**Then**: 404 で `ENTRY_NOT_FOUND` が返る

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0011` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- POST のレスポンスは 201 Created を使用する
- タグの小文字正規化は TagService 側で行う（Route Handler では行わない）

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 2 | 0 | 0 | 2 |

- **総項目数**: 4項目
- 🔵 **青信号**: 4項目 (100%)

**品質評価**: 高品質
