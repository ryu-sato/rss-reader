# TASK-0014: EntryFilterコンポーネント（components/entry-filter.tsx）

**タスクID**: TASK-0014
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 3 - フロントエンド実装
**信頼性レベル**: 🔵 *要件定義 REQ-004, REQ-005・設計文書より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-entry-view/requirements.md) - REQ-004, REQ-005
- **設計文書**: [📐 architecture.md](../../design/rss-entry-view/architecture.md)

## タスク概要

`src/components/entry-filter.tsx` を Client Component として実装する。フィード別・タグ別フィルターをドロップダウンで提供し、URLクエリパラメータ（feedId, tagId）を更新することでフィルタリングを実現する。

## 依存タスク

- **前提タスク**: [TASK-0002: エントリー型定義作成](TASK-0002.md)
- **後続タスク**: [TASK-0012: エントリー一覧ページ](TASK-0012.md)

## 完了条件

- [ ] `src/components/entry-filter.tsx` が実装されている
- [ ] フィード一覧がドロップダウンで表示され選択できる（REQ-004）
- [ ] タグ一覧がドロップダウンで表示され選択できる（REQ-005）
- [ ] フィルター選択で `?feedId=xxx` または `?tagId=xxx` URLパラメータが更新される
- [ ] フィルタークリアボタンが機能する
- [ ] TypeScript エラーなし

---

## 実装詳細

### 1. フィードフィルタードロップダウン 🔵

**信頼性**: 🔵 *REQ-004・設計文書 URLクエリパラメータより*

**実装ファイル**: `src/components/entry-filter.tsx`

```typescript
'use client'
import { useRouter, useSearchParams } from 'next/navigation'

export function EntryFilter() {
  const router = useRouter()
  const searchParams = useSearchParams()

  const setFilter = (key: string, value: string | null) => {
    const params = new URLSearchParams(searchParams.toString())
    if (value) {
      params.set(key, value)
    } else {
      params.delete(key)
    }
    params.set('page', '1') // フィルター変更時はページを1に戻す
    router.push(`/?${params.toString()}`)
  }

  return (
    <div>
      {/* フィードフィルター（フィード一覧はAPIから取得） */}
      {/* タグフィルター（タグ一覧はAPIから取得） */}
    </div>
  )
}
```

### 2. タグフィルタードロップダウン 🔵

**信頼性**: 🔵 *REQ-005より*

- `GET /api/tags` でタグ一覧を取得
- shadcn/ui の Select コンポーネントを使用

### 3. フィルタークリア 🟡

**信頼性**: 🟡 *UI設計から妥当な推測*

- 「クリア」ボタンで feedId・tagId パラメータを除去

---

## UI/UX要件

### ローディング状態 🟡

**信頼性**: 🟡 *一般的なUI設計から推測*

- フィード・タグ一覧取得中はドロップダウンを disabled

### モバイル対応 🟡

**信頼性**: 🟡 *一般的なレスポンシブデザインから推測*

- モバイルでもドロップダウンが使いやすいサイズ

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0014` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- フィルター変更時はページを1にリセットすること
- フィード一覧は既存の `GET /api/feeds` を使用すること

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 1 | 0 | 3 |
| UI/UX要件 | 0 | 2 | 0 | 2 |

- **総項目数**: 5項目
- 🔵 **青信号**: 2項目 (40%)
- 🟡 **黄信号**: 3項目 (60%)

**品質評価**: 要改善（UI詳細の確認推奨）
