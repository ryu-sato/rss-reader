# TASK-0017: E2Eシナリオ統合テスト

**タスクID**: TASK-0017
**タスクタイプ**: TDD
**推定工数**: 8時間
**フェーズ**: Phase 4 - 統合テスト・仕上げ
**信頼性レベル**: 🟡 *要件定義全体・設計文書より（具体的なE2E構成は推測含む）*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-entry-view/requirements.md) - 全REQ
- **設計文書**: [📐 architecture.md](../../design/rss-entry-view/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/rss-entry-view/dataflow.md)

## タスク概要

エントリー閲覧機能全体の統合テストを実装する。エントリー一覧表示・フィルタリング・モーダル表示・既読管理・後で読む・タグ付けの主要フローが正しく連携することを確認する。

## 依存タスク

- **前提タスク**: [TASK-0015: EntryModalコンポーネント](TASK-0015.md), [TASK-0016: TagInputコンポーネント](TASK-0016.md), [TASK-0012: エントリー一覧ページ](TASK-0012.md)
- **後続タスク**: なし（最終タスクの一つ）

## 完了条件

- [ ] エントリー一覧表示の統合テストが通る（REQ-001, REQ-002, REQ-003）
- [ ] フィルタリング（feedId AND tagId）の統合テストが通る（REQ-004, REQ-005）
- [ ] モーダル表示・自動既読の統合テストが通る（REQ-006, REQ-101）
- [ ] 既読・後で読むの更新テストが通る（REQ-008, REQ-009）
- [ ] タグ付け・解除の統合テストが通る（REQ-010, REQ-011, REQ-012）
- [ ] すべての統合テストが CI で通る

---

## 実装詳細

### 1. エントリー一覧表示フロー 🔵

**信頼性**: 🔵 *REQ-001, REQ-002, REQ-003・データフロー図 機能1より*

**テストファイル**: `src/__tests__/integration/entry-list.test.ts`

```typescript
// テスト例
describe('エントリー一覧表示', () => {
  it('全フィードのエントリーを公開日時降順で表示する', async () => {
    // DBにテストデータ作成
    // API呼び出し
    // レスポンス検証（順序・ページネーション）
  })

  it('feedId と tagId を AND 条件でフィルタリングする', async () => {
    // フィルター条件でAPIを呼び出し
    // AND条件の結果を検証
  })
})
```

### 2. モーダル表示・メタ情報更新フロー 🔵

**信頼性**: 🔵 *REQ-006, REQ-101, REQ-008, REQ-009・データフロー図 機能2, 4より*

```typescript
describe('モーダル・メタ情報', () => {
  it('既読なしエントリーのメタ情報を更新（既読）できる', async () => {
    // EntryMeta なし → PUT /api/entries/:id/meta { isRead: true }
    // → EntryMeta 新規作成・isRead: true を確認
  })

  it('後で読むフラグを設定・解除できる', async () => {
    // PUT /api/entries/:id/meta { isReadLater: true }
    // → isReadLater: true を確認
    // PUT /api/entries/:id/meta { isReadLater: false }
    // → isReadLater: false を確認
  })
})
```

### 3. タグ付けフロー 🔵

**信頼性**: 🔵 *REQ-010, REQ-011, REQ-012・データフロー図 機能5より*

```typescript
describe('タグ付け', () => {
  it('新規タグを作成してエントリーに付与できる', async () => {
    // POST /api/tags { name: "NewTag", entryId }
    // → タグが "newtag" として作成・付与される
  })

  it('既存タグをエントリーに付与できる（大文字小文字無視）', async () => {
    // 既存タグ "tech" に "TECH" で POST
    // → 既存タグが使用される
  })

  it('エントリーからタグを解除できる', async () => {
    // DELETE /api/tags/:tagId/entries/:entryId
    // → EntryTag が削除される
  })
})
```

---

## 統合テスト要件

### データベース状態管理 🟡

**信頼性**: 🟡 *テスト設計の一般的なパターンから推測*

- テスト前にDBをクリーンアップ
- テスト用データを `beforeEach` で挿入
- テスト後のクリーンアップ

### テスト環境 🟡

**信頼性**: 🟡 *既存テスト設定から推測*

- 既存プロジェクトのテスト設定（Jest/Vitest）を使用
- テスト用SQLiteを使用（本番DBとは分離）

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0017` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- 単体テストとは別にAPIレベルの統合テストを実装すること
- テスト間のDB状態の干渉を防ぐこと

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 3 | 0 | 0 | 3 |
| 統合テスト | 0 | 2 | 0 | 2 |

- **総項目数**: 5項目
- 🔵 **青信号**: 3項目 (60%)
- 🟡 **黄信号**: 2項目 (40%)

**品質評価**: 高品質
