# TASK-0018: 定期取得動作確認・エラーハンドリングテスト

**タスクID**: TASK-0018
**タスクタイプ**: TDD
**推定工数**: 6時間
**フェーズ**: Phase 4 - 統合テスト・仕上げ
**信頼性レベル**: 🟡 *要件定義 REQ-211, REQ-102, REQ-103, REQ-104・データフロー図より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-entry-view/requirements.md) - REQ-211, REQ-102, REQ-103, REQ-104, EDGE-004, NFR-002, NFR-102
- **設計文書**: [📐 architecture.md](../../design/rss-entry-view/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/rss-entry-view/dataflow.md)

## タスク概要

Cron + EntryFetcher + EntryService の結合テストを実装する。定期取得の正常フロー・ネットワーク失敗時の継続処理・SSRF違反時のスキップ・500件超過時の削除を確認する。

## 依存タスク

- **前提タスク**: [TASK-0008: CronScheduler実装](TASK-0008.md), [TASK-0005: EntryService保存・重複排除](TASK-0005.md)
- **後続タスク**: なし（最終タスクの一つ）

## 完了条件

- [ ] `fetchAllFeedsEntries()` の正常系テストが通る
- [ ] 一部フィード取得失敗時も他フィードの処理が継続することを確認する（REQ-104, EDGE-004）
- [ ] SSRF違反URLを持つフィードがスキップされる（NFR-102）
- [ ] 500件超過時に古いエントリーが削除される（REQ-102, REQ-401）
- [ ] 重複エントリー（同一guid）が二重登録されない（REQ-103）

---

## 実装詳細

### 1. fetchAllFeedsEntries 結合テスト 🔵

**信頼性**: 🔵 *REQ-211・データフロー図 機能6より*

**テストファイル**: `src/__tests__/integration/entry-fetcher.test.ts`

```typescript
describe('fetchAllFeedsEntries', () => {
  it('全フィードのエントリーを取得・保存する（正常系）', async () => {
    // モックRSSサーバーまたはfetch mock を使用
    // fetchAllFeedsEntries() を呼び出す
    // DBにエントリーが保存されることを確認
  })

  it('一部フィード取得失敗時に他フィードの処理を継続する', async () => {
    // フィード1: 成功, フィード2: ネットワークエラー, フィード3: 成功
    // fetchAllFeedsEntries() を呼び出す
    // フィード1・3のエントリーがDBに保存されることを確認
  })
})
```

### 2. 500件上限テスト 🔵

**信頼性**: 🔵 *REQ-102, REQ-401より*

```typescript
describe('エントリー上限管理', () => {
  it('500件を超えたら古いエントリーが削除される', async () => {
    // フィードに501件を保存
    // enforceEntryLimit(feedId) を呼び出す
    // 500件になることを確認
    // 最古のエントリーが削除されることを確認
  })
})
```

### 3. SSRF対策テスト 🔵

**信頼性**: 🔵 *NFR-102より*

```typescript
describe('SSRF対策', () => {
  it('プライベートIPアドレスへの取得をスキップする', async () => {
    // フィードURL: http://192.168.1.1/feed.xml （プライベートIP）
    // fetchAllFeedsEntries() を呼び出す
    // エラーログが出力されること
    // エントリーが保存されないことを確認
  })
})
```

### 4. 重複排除テスト 🔵

**信頼性**: 🔵 *REQ-103より*

```typescript
describe('重複排除', () => {
  it('同一guidのエントリーを二重登録しない', async () => {
    // 同じguidのエントリーを2回保存
    // DBの件数が1件のままであることを確認
  })
})
```

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0018` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- 外部RSSサーバーへの実際のHTTPリクエストは避け、モック（jest.mock / msw 等）を使用する
- SSRF テストは実際のネットワークアクセスを行わずに検証すること

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 4 | 0 | 0 | 4 |
| 統合テスト | 0 | 1 | 0 | 1 |

- **総項目数**: 5項目
- 🔵 **青信号**: 4項目 (80%)
- 🟡 **黄信号**: 1項目 (20%)

**品質評価**: 高品質
