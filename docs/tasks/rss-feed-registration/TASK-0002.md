# TASK-0002: Prisma + SQLiteセットアップ・DBスキーマ

**タスクID**: TASK-0002
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *design-interview.md Q1(Prisma), database-schema.sqlより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-feed-registration/requirements.md) - REQ-401, REQ-403
- **設計文書**: [📐 architecture.md](../../design/rss-feed-registration/architecture.md)
- **DBスキーマ**: [🗄️ database-schema.sql](../../design/rss-feed-registration/database-schema.sql)

## タスク概要

Prisma ORM の初期化、SQLite データベースの設定、`feeds` テーブルのスキーマ定義とマイグレーション実行、
Prisma クライアントのシングルトン実装を行う。

## 依存タスク

- **前提タスク**: [TASK-0001: Next.jsプロジェクト初期化](TASK-0001.md)
- **後続タスク**: [TASK-0003: 型定義ファイル作成](TASK-0003.md)

## 完了条件

- [ ] `prisma/schema.prisma` にFeedモデルが定義されている
- [ ] `prisma migrate dev --name init` でマイグレーションが成功する
- [ ] `prisma/dev.db` SQLiteファイルが生成されている
- [ ] `lib/db.ts` に Prisma クライアントシングルトンが実装されている
- [ ] `.env` ファイルに `DATABASE_URL` が設定されている
- [ ] `.gitignore` に `prisma/dev.db` が追加されている

---

## 実装詳細

### 1. Prisma 初期化 🔵

**信頼性**: 🔵 *design-interview.md Q1: Prismaより*

```bash
npx prisma init --datasource-provider sqlite
```

### 2. schema.prisma のFeedモデル定義 🔵

**信頼性**: 🔵 *database-schema.sql・要件定義 データモデルより*

**実装ファイル**: `prisma/schema.prisma`

```prisma
// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Feed {
  id            String    @id @default(uuid())
  url           String    @unique
  title         String
  description   String?
  memo          String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  lastFetchedAt DateTime?

  @@map("feeds")
}
```

### 3. .env ファイルの設定 🔵

**信頼性**: 🔵 *database-schema.sql コメントより*

**実装ファイル**: `.env`

```
DATABASE_URL="file:./prisma/dev.db"
```

**実装ファイル**: `.env.example`

```
DATABASE_URL="file:./prisma/dev.db"
```

### 4. マイグレーション実行 🔵

**信頼性**: 🔵 *Prisma マイグレーション標準手順より*

```bash
npx prisma migrate dev --name init
```

実行後の確認:
- `prisma/migrations/` に初期マイグレーションが生成されている
- `prisma/dev.db` が作成されている

### 5. Prisma クライアントシングルトン実装 🔵

**信頼性**: 🔵 *architecture.md lib/db.ts 記載より*

**実装ファイル**: `lib/db.ts`

```typescript
import { PrismaClient } from '@prisma/client'

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined
}

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: process.env.NODE_ENV === 'development' ? ['query', 'error', 'warn'] : ['error'],
  })

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = prisma
```

### 6. .gitignore への追加 🔵

**信頼性**: 🔵 *architecture.md 注意事項より*

`.gitignore` に以下を追加:
```
# SQLite database
prisma/dev.db
prisma/dev.db-journal

# Environment variables
.env
```

---

## 単体テスト要件

### テストケース1: Prismaクライアントシングルトン 🟡

**信頼性**: 🟡 *シングルトンパターンのテストとして推測*

**テストファイル**: `lib/db.test.ts`

- 同じ Prisma インスタンスが返されることを確認（シングルトン）

---

## 実装手順

1. `/tsumiki:direct-setup` - 直接実装・設定
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 注意事項

- `prisma/dev.db` は `.gitignore` に追加しGit管理しない
- `.env` も `.gitignore` に追加済みであることを確認
- Prisma Client の型は `npx prisma generate` で生成されるが、`migrate dev` 実行時に自動実行される
- SQLite の `@default(uuid())` は Prisma が UUID を生成する（SQLite自身の機能ではなくPrismaの機能）

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 6 | 0 | 0 | 6 |
| テスト要件 | 0 | 1 | 0 | 1 |

### 全体評価

- **総項目数**: 7項目
- 🔵 **青信号**: 6項目 (86%)
- 🟡 **黄信号**: 1項目 (14%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
