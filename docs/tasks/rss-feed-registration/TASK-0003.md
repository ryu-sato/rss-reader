# TASK-0003: 型定義ファイル作成

**タスクID**: TASK-0003
**タスクタイプ**: DIRECT
**推定工数**: 4時間
**フェーズ**: Phase 1 - 基盤構築
**信頼性レベル**: 🔵 *interfaces.tsより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-feed-registration/requirements.md)
- **設計文書**: [📐 architecture.md](../../design/rss-feed-registration/architecture.md)
- **型定義**: [📝 interfaces.ts](../../design/rss-feed-registration/interfaces.ts)

## タスク概要

設計文書の `interfaces.ts` に基づいて `types/feed.ts` を実装する。
Feed エンティティ、API リクエスト/レスポンス型、エラーコード、フォーム状態型などをTypeScriptで定義する。

## 依存タスク

- **前提タスク**: [TASK-0002: Prisma + SQLiteセットアップ](TASK-0002.md)
- **後続タスク**: [TASK-0004: SSRFガード実装](TASK-0004.md)

## 完了条件

- [ ] `types/feed.ts` に全型定義が実装されている
- [ ] TypeScript のビルドエラーがない（`npx tsc --noEmit`）
- [ ] `Feed`, `FeedListItem`, `CreateFeedRequest`, `UpdateFeedRequest` 等の型が定義されている
- [ ] `ErrorCode` 型が全エラーコードを網羅している
- [ ] `ErrorResponse`, `FeedFormState` 等の共通型が定義されている

---

## 実装詳細

### 1. 型定義ファイル作成 🔵

**信頼性**: 🔵 *interfaces.ts 設計文書より*

**実装ファイル**: `types/feed.ts`

設計文書 `docs/design/rss-feed-registration/interfaces.ts` の内容をそのまま実装する。
主な型定義:

- `Feed` - RSSフィードエンティティ
- `FeedListItem` - 一覧表示用 DTO
- `CreateFeedRequest` - 登録リクエスト
- `UpdateFeedRequest` - 更新リクエスト
- `CreateFeedResponse`, `GetFeedsResponse`, `GetFeedResponse`, `UpdateFeedResponse`, `DeleteFeedResponse` - レスポンス型
- `ErrorResponse` - エラーレスポンス
- `ErrorCode` - エラーコード列挙型
- `FeedFormState` - フォーム状態
- `FetchedFeedInfo` - RSSフェッチャー内部型
- `SSRFValidationResult` - SSRF検証結果
- `CreateFeedInput`, `UpdateFeedInput` - Prisma入力型

### 2. カスタムエラークラスの作成 🟡

**信頼性**: 🟡 *エラーハンドリング設計から推測*

**実装ファイル**: `lib/errors.ts`

```typescript
export class AppError extends Error {
  constructor(
    public readonly code: string,
    message: string,
    public readonly statusCode: number = 400
  ) {
    super(message)
    this.name = 'AppError'
  }
}

export class ConflictError extends AppError {
  constructor(message: string) {
    super('FEED_ALREADY_EXISTS', message, 409)
  }
}

export class NotFoundError extends AppError {
  constructor(message: string) {
    super('FEED_NOT_FOUND', message, 404)
  }
}

export class FeedFetchError extends AppError {
  constructor(message: string) {
    super('FEED_FETCH_FAILED', message, 422)
  }
}

export class InvalidFeedFormatError extends AppError {
  constructor(message: string) {
    super('INVALID_FEED_FORMAT', message, 422)
  }
}

export class SSRFError extends AppError {
  constructor(message: string) {
    super('URL_NOT_ALLOWED', message, 400)
  }
}
```

---

## 単体テスト要件

### テストケース1: エラークラスのテスト 🔵

**信頼性**: 🔵 *エラーハンドリング要件 REQ-103, REQ-106より*

**テストファイル**: `lib/errors.test.ts`

**Given**: AppError のサブクラスを作成
**When**: new ConflictError("Feed already exists")
**Then**: code が "FEED_ALREADY_EXISTS"、statusCode が 409

---

## 実装手順

1. `/tsumiki:direct-setup` - 直接実装・設定
2. `/tsumiki:direct-verify` - 動作確認・品質確認

---

## 注意事項

- `types/feed.ts` は設計文書 `interfaces.ts` と同期を保つ
- Prismaが生成する型（`import { Feed } from '@prisma/client'`）とは別に、APIレスポンス用の型を定義する
- TypeScript strict mode での型チェックを通過すること

---

## 信頼性レベルサマリー

### 項目別信頼性

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1 | 1 | 0 | 2 |
| テスト要件 | 1 | 0 | 0 | 1 |

### 全体評価

- **総項目数**: 3項目
- 🔵 **青信号**: 2項目 (67%)
- 🟡 **黄信号**: 1項目 (33%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
