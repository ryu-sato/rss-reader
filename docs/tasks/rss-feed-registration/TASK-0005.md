# TASK-0005: RSSフェッチャー実装

**タスクID**: TASK-0005
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - バックエンド実装
**信頼性レベル**: 🔵 *architecture.md・dataflow.md・REQ-101, REQ-102, REQ-106, REQ-107より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-feed-registration/requirements.md) - REQ-101, REQ-102, REQ-106, REQ-107, NFR-001
- **設計文書**: [📐 architecture.md](../../design/rss-feed-registration/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/rss-feed-registration/dataflow.md)

## タスク概要

`lib/rss-fetcher.ts` を実装する。
指定URLに対して30秒のタイムアウトで HTTP GET リクエストを送信し、
`rss-parser` でRSS/Atom形式としてパースしてフィードのタイトル・説明を抽出する。
タイトルが空の場合はURLをフォールバックとして使用する（EDGE-003）。

## 依存タスク

- **前提タスク**: [TASK-0004: SSRFガード実装](TASK-0004.md)
- **後続タスク**: [TASK-0006: FeedServiceビジネスロジック実装](TASK-0006.md)

## 完了条件

- [ ] `lib/rss-fetcher.ts` が実装されている
- [ ] 30秒タイムアウトが実装されている（AbortController使用）
- [ ] RSS 2.0 形式のフィードからタイトル・説明が取得できる
- [ ] Atom 1.0 形式のフィードからタイトル・説明が取得できる
- [ ] HTTP取得失敗時に FeedFetchError がスローされる
- [ ] RSS/Atom形式でないコンテンツに対して InvalidFeedFormatError がスローされる
- [ ] タイトルが空の場合はURLをフォールバックとして使用する（EDGE-003）
- [ ] Vitest で単体テストがパスしている

---

## 実装詳細

### 1. RSSフェッチャーの実装 🔵

**信頼性**: 🔵 *architecture.md RSSパース・NFR-001 30秒タイムアウトより*

**実装ファイル**: `lib/rss-fetcher.ts`

```typescript
import Parser from 'rss-parser'
import { FeedFetchError, InvalidFeedFormatError } from './errors'
import type { FetchedFeedInfo } from '@/types/feed'

const FETCH_TIMEOUT_MS = 30_000 // 30秒 (NFR-001)
const parser = new Parser()

export async function fetchFeed(url: string): Promise<FetchedFeedInfo> {
  const controller = new AbortController()
  const timeoutId = setTimeout(() => controller.abort(), FETCH_TIMEOUT_MS)

  let response: Response
  try {
    response = await fetch(url, { signal: controller.signal })
  } catch (err) {
    if (err instanceof Error && err.name === 'AbortError') {
      throw new FeedFetchError('Failed to fetch the feed URL')
    }
    throw new FeedFetchError('Failed to fetch the feed URL')
  } finally {
    clearTimeout(timeoutId)
  }

  if (!response.ok) {
    throw new FeedFetchError('Failed to fetch the feed URL')
  }

  const text = await response.text()

  let feed: Parser.Output<Record<string, unknown>>
  try {
    feed = await parser.parseString(text)
  } catch {
    throw new InvalidFeedFormatError('URL is not a valid RSS/Atom feed')
  }

  // EDGE-003: タイトルが空の場合はURLをフォールバックとして使用
  const title = feed.title?.trim() || url
  const description = feed.description?.trim() || null

  return {
    title,
    description,
    lastFetchedAt: new Date(),
  }
}
```

---

## 単体テスト要件

### テストケース1: 有効なRSS 2.0フィードの取得 🔵

**信頼性**: 🔵 *REQ-101, REQ-102より*

**テストファイル**: `lib/rss-fetcher.test.ts`

**Given**: 有効なRSS 2.0 XMLコンテンツを返すモックfetch
**When**: fetchFeed(url) を呼び出す
**Then**: `{ title: "Example Blog", description: "Blog description", lastFetchedAt: Date }` が返される

### テストケース2: Atom 1.0フィードの取得 🟡

**信頼性**: 🟡 *rss-parserのAtom対応から推測*

**Given**: 有効なAtom 1.0 XMLコンテンツを返すモックfetch
**When**: fetchFeed(url) を呼び出す
**Then**: タイトルと説明が正しく取得される

### テストケース3: HTTP取得失敗時のエラー 🔵

**信頼性**: 🔵 *REQ-106より*

**Given**: fetch がネットワークエラーをスローするモック
**When**: fetchFeed(url) を呼び出す
**Then**: FeedFetchError がスローされる（code: "FEED_FETCH_FAILED"）

### テストケース4: タイムアウト時のエラー 🔵

**信頼性**: 🔵 *NFR-001より*

**Given**: AbortError をスローするモックfetch（タイムアウトシミュレート）
**When**: fetchFeed(url) を呼び出す
**Then**: FeedFetchError がスローされる

### テストケース5: RSS形式でないコンテンツのエラー 🔵

**信頼性**: 🔵 *REQ-107より*

**Given**: HTMLコンテンツを返すモックfetch
**When**: fetchFeed(url) を呼び出す
**Then**: InvalidFeedFormatError がスローされる（code: "INVALID_FEED_FORMAT"）

### テストケース6: タイトルが空の場合のフォールバック 🔵

**信頼性**: 🔵 *EDGE-003より*

**Given**: タイトルが空のRSSフィードを返すモックfetch
**When**: fetchFeed("https://example.com/feed.xml") を呼び出す
**Then**: title が "https://example.com/feed.xml"（URLのフォールバック）になる

### テストケース7: 説明がnullの場合 🟡

**信頼性**: 🟡 *データモデルから推測*

**Given**: 説明（description）がないRSSフィードを返すモックfetch
**When**: fetchFeed(url) を呼び出す
**Then**: description が null になる

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0005` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- `fetch` のモックには `vi.stubGlobal('fetch', vi.fn())` を使用
- `rss-parser` のモックには `vi.mock('rss-parser')` を使用
- タイムアウトテストには `vi.useFakeTimers()` を使用する

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1 | 0 | 0 | 1 |
| 単体テスト | 5 | 2 | 0 | 7 |

- **総項目数**: 8項目
- 🔵 **青信号**: 6項目 (75%)
- 🟡 **黄信号**: 2項目 (25%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
