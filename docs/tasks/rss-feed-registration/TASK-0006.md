# TASK-0006: FeedServiceビジネスロジック実装

**タスクID**: TASK-0006
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - バックエンド実装
**信頼性レベル**: 🔵 *architecture.md サービス層・dataflow.md・全要件より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-feed-registration/requirements.md) - REQ-101〜107, REQ-401〜404
- **設計文書**: [📐 architecture.md](../../design/rss-feed-registration/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/rss-feed-registration/dataflow.md)

## タスク概要

`lib/feed-service.ts` を実装する。
フィードのCRUD操作（作成・取得・一覧・更新・削除）のビジネスロジックを担当する。
フィード作成時には重複チェック → SSRF検証 → RSSフェッチ → DB保存の順序で処理する。

## 依存タスク

- **前提タスク**: [TASK-0005: RSSフェッチャー実装](TASK-0005.md)
- **後続タスク**: [TASK-0007: APIルートハンドラー（一覧・登録）](TASK-0007.md)

## 完了条件

- [ ] `lib/feed-service.ts` が実装されている
- [ ] `createFeed()`: 重複チェック → SSRF検証 → RSSフェッチ → DB保存の全フローが動作する
- [ ] `getAllFeeds()`: 全フィードを登録日時降順で取得できる
- [ ] `getFeedById()`: IDでフィードを取得でき、存在しない場合はNotFoundErrorをスローする
- [ ] `updateFeed()`: タイトル・説明・メモを更新できる
- [ ] `deleteFeed()`: フィードをDBから削除できる
- [ ] Vitest で単体テストがパスしている（Prismaはモック）

---

## 実装詳細

### 1. FeedService の実装 🔵

**信頼性**: 🔵 *dataflow.md フィード登録フロー・architecture.md サービス層より*

**実装ファイル**: `lib/feed-service.ts`

主要メソッド:

```typescript
import { prisma } from './db'
import { validateUrl } from './ssrf-guard'
import { fetchFeed } from './rss-fetcher'
import { ConflictError, NotFoundError } from './errors'
import type { Feed } from '@prisma/client'
import type { CreateFeedInput, UpdateFeedInput, FeedListItem } from '@/types/feed'

export async function createFeed(url: string): Promise<Feed> {
  // 1. 重複チェック (REQ-103, REQ-401)
  const existing = await prisma.feed.findUnique({ where: { url } })
  if (existing) {
    throw new ConflictError('Feed with this URL already exists')
  }

  // 2. SSRF検証 (NFR-101)
  await validateUrl(url)

  // 3. RSSフェッチ (REQ-101, REQ-102)
  const feedInfo = await fetchFeed(url)

  // 4. DB保存
  return prisma.feed.create({
    data: {
      url,
      title: feedInfo.title,
      description: feedInfo.description,
      lastFetchedAt: feedInfo.lastFetchedAt,
    },
  })
}

export async function getAllFeeds(): Promise<FeedListItem[]> {
  // REQ-002, REQ-003
  const feeds = await prisma.feed.findMany({
    orderBy: { createdAt: 'desc' },
    select: { id: true, title: true, url: true, createdAt: true, updatedAt: true },
  })
  return feeds
}

export async function getFeedById(id: string): Promise<Feed> {
  const feed = await prisma.feed.findUnique({ where: { id } })
  if (!feed) throw new NotFoundError('Feed not found')
  return feed
}

export async function updateFeed(id: string, data: UpdateFeedInput): Promise<Feed> {
  // 存在確認
  await getFeedById(id)
  return prisma.feed.update({ where: { id }, data })
}

export async function deleteFeed(id: string): Promise<void> {
  await getFeedById(id)
  await prisma.feed.delete({ where: { id } })
}
```

---

## 単体テスト要件

### テストケース1: フィード登録の正常系 🔵

**信頼性**: 🔵 *REQ-101, REQ-102より*

**テストファイル**: `lib/feed-service.test.ts`

**Given**: 新規URL、SSRFガードOK、RSSフェッチ成功
**When**: createFeed("https://example.com/feed.xml") を呼び出す
**Then**: Feedオブジェクトが返される、prisma.feed.createが呼ばれる

### テストケース2: 重複URLのエラー 🔵

**信頼性**: 🔵 *REQ-103より*

**Given**: 既登録URLがprisma.feed.findUniqueで返される
**When**: createFeed(url) を呼び出す
**Then**: ConflictError がスローされる（code: "FEED_ALREADY_EXISTS"）

### テストケース3: SSRF検証エラーの伝播 🔵

**信頼性**: 🔵 *NFR-101より*

**Given**: validateUrl がSSRFErrorをスロー
**When**: createFeed(url) を呼び出す
**Then**: SSRFError がそのまま上位に伝播する

### テストケース4: フィード一覧取得 🔵

**信頼性**: 🔵 *REQ-002, REQ-003より*

**Given**: DBに3件のフィードがある
**When**: getAllFeeds() を呼び出す
**Then**: FeedListItem[]（登録日時降順）が返される

### テストケース5: フィード取得（存在しない）🔵

**信頼性**: 🔵 *API仕様 404エラーより*

**Given**: 存在しないID
**When**: getFeedById("non-existent-id") を呼び出す
**Then**: NotFoundError がスローされる

### テストケース6: フィード更新 🔵

**信頼性**: 🔵 *REQ-104より*

**Given**: 既存のFeed
**When**: updateFeed(id, { title: "New Title", memo: "note" }) を呼び出す
**Then**: 更新されたFeedが返される

### テストケース7: フィード削除 🔵

**信頼性**: 🔵 *REQ-105より*

**Given**: 既存のFeed
**When**: deleteFeed(id) を呼び出す
**Then**: prisma.feed.deleteが呼ばれる、エラーなく完了

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0006` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 注意事項

- Prismaのモックには `vi.mock('@/lib/db')` を使用
- SSRFガード・RSSフェッチャーもモック化してユニットテストを独立させる
- `UpdateFeedInput` はundefinedフィールドをPrismaに渡さないよう undefined を除去する

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1 | 0 | 0 | 1 |
| 単体テスト | 7 | 0 | 0 | 7 |

- **総項目数**: 8項目
- 🔵 **青信号**: 8項目 (100%)
- 🟡 **黄信号**: 0項目 (0%)
- 🔴 **赤信号**: 0項目 (0%)

**品質評価**: 高品質
