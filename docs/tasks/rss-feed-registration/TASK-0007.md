# TASK-0007: APIルートハンドラー（フィード一覧取得・登録）

**タスクID**: TASK-0007
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - バックエンド実装
**信頼性レベル**: 🔵 *api-endpoints.md GET /api/feeds・POST /api/feedsより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-feed-registration/requirements.md) - REQ-001〜107
- **設計文書**: [📐 architecture.md](../../design/rss-feed-registration/architecture.md)
- **API仕様**: [🔌 api-endpoints.md](../../design/rss-feed-registration/api-endpoints.md)

## タスク概要

`app/api/feeds/route.ts` を実装する。
- `GET /api/feeds`: 全フィード一覧を返す
- `POST /api/feeds`: URLバリデーション後にフィードを登録する

## 依存タスク

- **前提タスク**: [TASK-0006: FeedServiceビジネスロジック実装](TASK-0006.md)
- **後続タスク**: [TASK-0008: APIルートハンドラー（詳細・更新・削除）](TASK-0008.md)

## 完了条件

- [ ] `app/api/feeds/route.ts` が実装されている
- [ ] GET /api/feeds が `{ success: true, data: FeedListItem[] }` を返す
- [ ] POST /api/feeds が URL バリデーション → FeedService.createFeed → 201レスポンスを返す
- [ ] 各エラーコードが適切な HTTP ステータスコードにマッピングされる
- [ ] Vitest で単体テストがパスしている

---

## 実装詳細

### 1. GET /api/feeds, POST /api/feeds の実装 🔵

**信頼性**: 🔵 *api-endpoints.md・architecture.md Route Handlersより*

**実装ファイル**: `app/api/feeds/route.ts`

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { getAllFeeds, createFeed } from '@/lib/feed-service'
import { AppError } from '@/lib/errors'

export async function GET() {
  try {
    const feeds = await getAllFeeds()
    return NextResponse.json({ success: true, data: feeds })
  } catch (error) {
    return handleError(error)
  }
}

export async function POST(request: NextRequest) {
  try {
    const body = await request.json()
    const { url } = body

    // URL必須チェック
    if (!url || typeof url !== 'string') {
      return NextResponse.json(
        { success: false, error: { code: 'VALIDATION_ERROR', message: 'URL is required' } },
        { status: 400 }
      )
    }

    const feed = await createFeed(url)
    return NextResponse.json({ success: true, data: feed }, { status: 201 })
  } catch (error) {
    return handleError(error)
  }
}

function handleError(error: unknown): NextResponse {
  if (error instanceof AppError) {
    return NextResponse.json(
      { success: false, error: { code: error.code, message: error.message } },
      { status: error.statusCode }
    )
  }
  console.error('Unexpected error:', error)
  return NextResponse.json(
    { success: false, error: { code: 'INTERNAL_SERVER_ERROR', message: 'Internal server error' } },
    { status: 500 }
  )
}
```

---

## 単体テスト要件

### テストケース1: GET /api/feeds 正常系 🔵

**信頼性**: 🔵 *api-endpoints.md GET /api/feedsより*

**テストファイル**: `app/api/feeds/route.test.ts`

**Given**: FeedServiceがFeedListItem[]を返す
**When**: GET() を呼び出す
**Then**: 200, { success: true, data: [...] } が返される

### テストケース2: POST /api/feeds 正常系 🔵

**信頼性**: 🔵 *api-endpoints.md POST /api/feedsより*

**Given**: FeedServiceがFeedを返す
**When**: POST({ url: "https://example.com/feed.xml" }) を呼び出す
**Then**: 201, { success: true, data: Feed } が返される

### テストケース3: POST - URL未入力 🔵

**信頼性**: 🔵 *api-endpoints.md バリデーションより*

**Given**: body に url がない
**When**: POST({}) を呼び出す
**Then**: 400, { error.code: "VALIDATION_ERROR" } が返される

### テストケース4: POST - 重複URL (409) 🔵

**信頼性**: 🔵 *api-endpoints.md FEED_ALREADY_EXISTSより*

**Given**: FeedServiceがConflictErrorをスロー
**When**: POST({ url: "..." }) を呼び出す
**Then**: 409, { error.code: "FEED_ALREADY_EXISTS" } が返される

### テストケース5: POST - RSSフェッチ失敗 (422) 🔵

**信頼性**: 🔵 *api-endpoints.md FEED_FETCH_FAILEDより*

**Given**: FeedServiceがFeedFetchErrorをスロー
**When**: POST({ url: "..." }) を呼び出す
**Then**: 422, { error.code: "FEED_FETCH_FAILED" } が返される

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0007` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1 | 0 | 0 | 1 |
| 単体テスト | 5 | 0 | 0 | 5 |

- **総項目数**: 6項目
- 🔵 **青信号**: 6項目 (100%)
- **品質評価**: 高品質
