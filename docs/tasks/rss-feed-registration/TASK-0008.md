# TASK-0008: APIルートハンドラー（フィード詳細取得・更新・削除）

**タスクID**: TASK-0008
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 2 - バックエンド実装
**信頼性レベル**: 🔵 *api-endpoints.md GET/PUT/DELETE /api/feeds/:idより*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-feed-registration/requirements.md) - REQ-104, REQ-105
- **設計文書**: [📐 architecture.md](../../design/rss-feed-registration/architecture.md)
- **API仕様**: [🔌 api-endpoints.md](../../design/rss-feed-registration/api-endpoints.md)

## タスク概要

`app/api/feeds/[id]/route.ts` を実装する。
- `GET /api/feeds/:id`: フィード詳細を返す
- `PUT /api/feeds/:id`: フィード情報を更新する
- `DELETE /api/feeds/:id`: フィードを削除する

## 依存タスク

- **前提タスク**: [TASK-0007: APIルートハンドラー（一覧・登録）](TASK-0007.md)
- **後続タスク**: [TASK-0009: フィード一覧ページ](TASK-0009.md)

## 完了条件

- [ ] `app/api/feeds/[id]/route.ts` が実装されている
- [ ] GET /api/feeds/:id が Feed 詳細または 404 を返す
- [ ] PUT /api/feeds/:id がバリデーション後にフィードを更新する
- [ ] DELETE /api/feeds/:id がフィードを削除する
- [ ] すべての404エラーが `{ success: false, error: { code: "FEED_NOT_FOUND" } }` を返す
- [ ] Vitest で単体テストがパスしている

---

## 実装詳細

### 1. GET/PUT/DELETE /api/feeds/:id の実装 🔵

**信頼性**: 🔵 *api-endpoints.md・architecture.mdより*

**実装ファイル**: `app/api/feeds/[id]/route.ts`

```typescript
import { NextRequest, NextResponse } from 'next/server'
import { getFeedById, updateFeed, deleteFeed } from '@/lib/feed-service'
import { AppError } from '@/lib/errors'

type Params = { params: { id: string } }

export async function GET(_: NextRequest, { params }: Params) {
  try {
    const feed = await getFeedById(params.id)
    return NextResponse.json({ success: true, data: feed })
  } catch (error) {
    return handleError(error)
  }
}

export async function PUT(request: NextRequest, { params }: Params) {
  try {
    const body = await request.json()
    const { title, description, memo } = body

    // バリデーション: titleが空文字不可
    if (title !== undefined && title.trim() === '') {
      return NextResponse.json(
        { success: false, error: { code: 'VALIDATION_ERROR', message: 'Title cannot be empty' } },
        { status: 400 }
      )
    }

    const feed = await updateFeed(params.id, { title, description, memo })
    return NextResponse.json({ success: true, data: feed })
  } catch (error) {
    return handleError(error)
  }
}

export async function DELETE(_: NextRequest, { params }: Params) {
  try {
    await deleteFeed(params.id)
    return NextResponse.json({ success: true })
  } catch (error) {
    return handleError(error)
  }
}

function handleError(error: unknown): NextResponse {
  if (error instanceof AppError) {
    return NextResponse.json(
      { success: false, error: { code: error.code, message: error.message } },
      { status: error.statusCode }
    )
  }
  console.error('Unexpected error:', error)
  return NextResponse.json(
    { success: false, error: { code: 'INTERNAL_SERVER_ERROR', message: 'Internal server error' } },
    { status: 500 }
  )
}
```

---

## 単体テスト要件

### テストケース1: GET - 正常系 🔵

**信頼性**: 🔵 *api-endpoints.md GET /api/feeds/:idより*

**Given**: FeedServiceがFeedを返す
**When**: GET(id) を呼び出す
**Then**: 200, { success: true, data: Feed } が返される

### テストケース2: GET - 404 🔵

**信頼性**: 🔵 *api-endpoints.md 404エラーより*

**Given**: FeedServiceがNotFoundErrorをスロー
**When**: GET(id) を呼び出す
**Then**: 404, { error.code: "FEED_NOT_FOUND" } が返される

### テストケース3: PUT - 正常系 🔵

**信頼性**: 🔵 *api-endpoints.md PUT /api/feeds/:idより*

**Given**: FeedServiceが更新後のFeedを返す
**When**: PUT(id, { title: "New Title", memo: "note" }) を呼び出す
**Then**: 200, { success: true, data: Feed } が返される

### テストケース4: PUT - タイトルが空文字 🔵

**信頼性**: 🔵 *api-endpoints.md バリデーションより*

**Given**: body.title = ""
**When**: PUT(id, { title: "" }) を呼び出す
**Then**: 400, { error.code: "VALIDATION_ERROR" } が返される

### テストケース5: DELETE - 正常系 🔵

**信頼性**: 🔵 *api-endpoints.md DELETE /api/feeds/:idより*

**Given**: FeedServiceが削除成功
**When**: DELETE(id) を呼び出す
**Then**: 200, { success: true } が返される

### テストケース6: DELETE - 404 🔵

**信頼性**: 🔵 *api-endpoints.md 404エラーより*

**Given**: FeedServiceがNotFoundErrorをスロー
**When**: DELETE(id) を呼び出す
**Then**: 404, { error.code: "FEED_NOT_FOUND" } が返される

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0008` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1 | 0 | 0 | 1 |
| 単体テスト | 6 | 0 | 0 | 6 |

- **総項目数**: 7項目
- 🔵 **青信号**: 7項目 (100%)
- **品質評価**: 高品質
