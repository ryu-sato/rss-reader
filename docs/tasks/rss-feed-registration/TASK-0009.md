# TASK-0009: フィード一覧ページ

**タスクID**: TASK-0009
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - フロントエンド実装
**信頼性レベル**: 🔵 *architecture.md ディレクトリ構造・user-stories.md 2.1, 2.2より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-feed-registration/requirements.md) - REQ-002, REQ-003, REQ-202
- **設計文書**: [📐 architecture.md](../../design/rss-feed-registration/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/rss-feed-registration/dataflow.md)

## タスク概要

`app/page.tsx`（フィード一覧ページ）と `components/feed-list.tsx`（フィード一覧コンポーネント）を実装する。
React Server Component としてDBから直接フィード一覧を取得し、タイトル・登録日時・最終更新日時を表示する。
フィードが0件の場合は案内メッセージを表示する。

## 依存タスク

- **前提タスク**: [TASK-0008: APIルートハンドラー（詳細・更新・削除）](TASK-0008.md)
- **後続タスク**: [TASK-0010: フィード登録フォームコンポーネント](TASK-0010.md)

## 完了条件

- [ ] `app/page.tsx` が React Server Component として実装されている
- [ ] `components/feed-list.tsx` が実装されている
- [ ] フィード一覧にタイトル・登録日時・最終更新日時が表示される（REQ-003）
- [ ] フィードが0件の場合に「No feeds registered yet. Add your first feed!」等のメッセージが表示される（REQ-202）
- [ ] 各フィードに編集ボタン・削除ボタンが表示される
- [ ] フィード登録ページへのリンクがある
- [ ] Vitest + React Testing Library でテストがパスしている

---

## 実装詳細

### 1. フィード一覧ページ（React Server Component）🔵

**信頼性**: 🔵 *architecture.md React Server Components・dataflow.md 機能2より*

**実装ファイル**: `app/page.tsx`

```typescript
import { getAllFeeds } from '@/lib/feed-service'
import FeedList from '@/components/feed-list'
import Link from 'next/link'

export default async function Home() {
  const feeds = await getAllFeeds()

  return (
    <main className="container mx-auto py-8 px-4">
      <div className="flex justify-between items-center mb-6">
        <h1 className="text-2xl font-bold">RSS Feeds</h1>
        <Link href="/feeds/new">
          <Button>Add Feed</Button>
        </Link>
      </div>
      <FeedList feeds={feeds} />
    </main>
  )
}
```

### 2. フィード一覧コンポーネント 🔵

**信頼性**: 🔵 *REQ-002, REQ-003・user-stories.md 2.1, 2.2より*

**実装ファイル**: `components/feed-list.tsx`

```typescript
'use client'

import type { FeedListItem } from '@/types/feed'
import DeleteConfirmDialog from './delete-confirm-dialog'
import Link from 'next/link'

interface FeedListProps {
  feeds: FeedListItem[]
}

export default function FeedList({ feeds }: FeedListProps) {
  if (feeds.length === 0) {
    return (
      <div className="text-center py-12 text-muted-foreground">
        <p>No feeds registered yet. Add your first feed!</p>
        <Link href="/feeds/new" className="mt-4 inline-block">
          <Button>Add Feed</Button>
        </Link>
      </div>
    )
  }

  return (
    <ul className="space-y-4">
      {feeds.map((feed) => (
        <li key={feed.id} className="border rounded-lg p-4 flex justify-between items-start">
          <div>
            <h2 className="font-semibold">{feed.title}</h2>
            <p className="text-sm text-muted-foreground truncate max-w-md">{feed.url}</p>
            <p className="text-xs text-muted-foreground mt-1">
              Added: {new Date(feed.createdAt).toLocaleDateString()}
              {' | '}
              Updated: {new Date(feed.updatedAt).toLocaleDateString()}
            </p>
          </div>
          <div className="flex gap-2">
            <Link href={`/feeds/${feed.id}/edit`}>
              <Button variant="outline" size="sm">Edit</Button>
            </Link>
            <DeleteConfirmDialog feedId={feed.id} feedTitle={feed.title} />
          </div>
        </li>
      ))}
    </ul>
  )
}
```

---

## 単体テスト要件

### テストケース1: フィード一覧の表示 🔵

**信頼性**: 🔵 *REQ-002, REQ-003より*

**テストファイル**: `components/feed-list.test.tsx`

**Given**: FeedListItem[] が3件渡される
**When**: FeedList コンポーネントをレンダリング
**Then**: 3つのフィードタイトルが表示される、登録日時・更新日時が表示される

### テストケース2: フィードが0件の場合のメッセージ 🔵

**信頼性**: 🔵 *REQ-202より*

**Given**: feeds = [] が渡される
**When**: FeedList コンポーネントをレンダリング
**Then**: 「No feeds registered yet.」メッセージが表示される

### テストケース3: 編集ボタンのリンク 🟡

**信頼性**: 🟡 *アーキテクチャ設計から推測*

**Given**: フィードリストが表示されている
**When**: 編集ボタンを確認
**Then**: `/feeds/${feed.id}/edit` へのリンクが含まれている

---

## UI/UX要件

### ローディング状態 🟡

**信頼性**: 🟡 *Next.js App Router Suspenseから推測*

- `app/loading.tsx` でローディングスケルトンを表示

### エラー表示 🟡

**信頼性**: 🟡 *Next.js App Router error.tsxから推測*

- `app/error.tsx` でエラー表示

### モバイル対応 🟡

**信頼性**: 🟡 *Tailwind CSS レスポンシブ設計から推測*

- `container mx-auto` でレスポンシブなコンテナ
- URLは `truncate max-w-md` で省略表示

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0009` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 2 | 1 | 0 | 3 |
| UI/UX要件 | 0 | 3 | 0 | 3 |

- **総項目数**: 8項目
- 🔵 **青信号**: 4項目 (50%)
- 🟡 **黄信号**: 4項目 (50%)
- **品質評価**: 高品質
