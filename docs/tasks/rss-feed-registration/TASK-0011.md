# TASK-0011: 削除確認ダイアログコンポーネント

**タスクID**: TASK-0011
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - フロントエンド実装
**信頼性レベル**: 🔵 *user-stories.md 4.1・REQ-105より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-feed-registration/requirements.md) - REQ-105
- **設計文書**: [📐 architecture.md](../../design/rss-feed-registration/architecture.md)
- **データフロー**: [🔄 dataflow.md](../../design/rss-feed-registration/dataflow.md)

## タスク概要

`components/delete-confirm-dialog.tsx` を実装する。
shadcn/ui の AlertDialog を使用して削除確認ダイアログを表示する。
「Delete」ボタンをクリックすると確認ダイアログが表示され、
「Delete」で削除API呼び出し、「Cancel」でキャンセルする（REQ-105）。

## 依存タスク

- **前提タスク**: [TASK-0010: フィード登録フォームコンポーネント](TASK-0010.md)
- **後続タスク**: [TASK-0012: フィード登録ページ](TASK-0012.md)

## 完了条件

- [ ] `components/delete-confirm-dialog.tsx` が実装されている
- [ ] 削除ボタンクリックで確認ダイアログが表示される（REQ-105）
- [ ] 「Delete」クリックで DELETE /api/feeds/:id が呼ばれ、一覧が更新される
- [ ] 「Cancel」クリックでダイアログが閉じ、削除されない
- [ ] 削除中はボタンが非活性になる
- [ ] Vitest + React Testing Library でテストがパスしている

---

## 実装詳細

### 1. 削除確認ダイアログの実装 🔵

**信頼性**: 🔵 *REQ-105・dataflow.md 機能4・design-interview.md Q4(shadcn/ui)より*

**実装ファイル**: `components/delete-confirm-dialog.tsx`

```typescript
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from '@/components/ui/alert-dialog'
import { Button } from '@/components/ui/button'

interface DeleteConfirmDialogProps {
  feedId: string
  feedTitle: string
}

export default function DeleteConfirmDialog({ feedId, feedTitle }: DeleteConfirmDialogProps) {
  const [isDeleting, setIsDeleting] = useState(false)
  const [open, setOpen] = useState(false)
  const router = useRouter()

  const handleDelete = async () => {
    setIsDeleting(true)
    try {
      const response = await fetch(`/api/feeds/${feedId}`, { method: 'DELETE' })
      if (response.ok) {
        setOpen(false)
        router.refresh()
      }
    } finally {
      setIsDeleting(false)
    }
  }

  return (
    <AlertDialog open={open} onOpenChange={setOpen}>
      <AlertDialogTrigger asChild>
        <Button variant="destructive" size="sm">Delete</Button>
      </AlertDialogTrigger>
      <AlertDialogContent>
        <AlertDialogHeader>
          <AlertDialogTitle>Are you sure?</AlertDialogTitle>
          <AlertDialogDescription>
            This will permanently delete &quot;{feedTitle}&quot;. This action cannot be undone.
          </AlertDialogDescription>
        </AlertDialogHeader>
        <AlertDialogFooter>
          <AlertDialogCancel disabled={isDeleting}>Cancel</AlertDialogCancel>
          <AlertDialogAction onClick={handleDelete} disabled={isDeleting}>
            {isDeleting ? 'Deleting...' : 'Delete'}
          </AlertDialogAction>
        </AlertDialogFooter>
      </AlertDialogContent>
    </AlertDialog>
  )
}
```

---

## 単体テスト要件

### テストケース1: 削除ボタンの表示 🔵

**信頼性**: 🔵 *REQ-105より*

**テストファイル**: `components/delete-confirm-dialog.test.tsx`

**Given**: DeleteConfirmDialogコンポーネントをレンダリング
**When**: 初期表示
**Then**: 「Delete」ボタンが表示されている

### テストケース2: 確認ダイアログの表示 🔵

**信頼性**: 🔵 *REQ-105 確認ダイアログありより*

**Given**: DeleteConfirmDialogコンポーネントが表示されている
**When**: 「Delete」ボタンをクリック
**Then**: 確認ダイアログが表示され「Are you sure?」が表示される

### テストケース3: キャンセルで削除なし 🔵

**信頼性**: 🔵 *REQ-105・user-stories.md 4.1より*

**Given**: 確認ダイアログが表示されている
**When**: 「Cancel」をクリック
**Then**: ダイアログが閉じ、fetch APIが呼ばれない

### テストケース4: 削除実行 🔵

**信頼性**: 🔵 *REQ-105・user-stories.md 4.1より*

**Given**: 成功レスポンスを返すモックfetch、確認ダイアログが表示されている
**When**: 「Delete」をクリック
**Then**: DELETE /api/feeds/:id が呼ばれ、router.refresh()が呼ばれる

### テストケース5: 削除中のローディング状態 🟡

**信頼性**: 🟡 *UX要件として推測*

**Given**: 削除処理中
**When**: 削除ボタンクリック後
**Then**: 「Deleting...」が表示され、ボタンが非活性になる

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0011` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 1 | 0 | 0 | 1 |
| 単体テスト | 4 | 1 | 0 | 5 |

- **総項目数**: 6項目
- 🔵 **青信号**: 5項目 (83%)
- 🟡 **黄信号**: 1項目 (17%)
- **品質評価**: 高品質
