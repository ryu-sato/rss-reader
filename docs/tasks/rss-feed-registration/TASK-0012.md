# TASK-0012: フィード登録ページ

**タスクID**: TASK-0012
**タスクタイプ**: TDD
**推定工数**: 4時間
**フェーズ**: Phase 3 - フロントエンド実装
**信頼性レベル**: 🔵 *architecture.md app/feeds/new/page.tsx・user-stories.md 1.1より*

## 関連文書

- **概要**: [📋 overview.md](overview.md)
- **要件定義**: [📋 requirements.md](../../spec/rss-feed-registration/requirements.md) - REQ-001, REQ-101〜107
- **設計文書**: [📐 architecture.md](../../design/rss-feed-registration/architecture.md)

## タスク概要

`app/feeds/new/page.tsx`（フィード登録ページ）を実装する。
`FeedForm` コンポーネントを組み込み、完全なフィード登録フローを提供する。
一覧ページへの戻りリンクも含める。

## 依存タスク

- **前提タスク**: [TASK-0011: 削除確認ダイアログコンポーネント](TASK-0011.md)
- **後続タスク**: [TASK-0013: フィード編集ページ](TASK-0013.md)

## 完了条件

- [ ] `app/feeds/new/page.tsx` が実装されている
- [ ] FeedFormコンポーネントが組み込まれている
- [ ] ページタイトル「Add New Feed」が表示される
- [ ] 一覧ページ（`/`）への戻りリンクがある
- [ ] 登録成功後に一覧ページへリダイレクトされる（router.push('/')）
- [ ] Vitest + React Testing Library でテストがパスしている

---

## 実装詳細

### 1. フィード登録ページの実装 🔵

**信頼性**: 🔵 *architecture.md ディレクトリ構造・user-stories.md 1.1より*

**実装ファイル**: `app/feeds/new/page.tsx`

```typescript
import FeedForm from '@/components/feed-form'
import Link from 'next/link'
import { ArrowLeft } from 'lucide-react'

export default function NewFeedPage() {
  return (
    <main className="container mx-auto py-8 px-4 max-w-2xl">
      <Link href="/" className="flex items-center gap-2 text-muted-foreground hover:text-foreground mb-6">
        <ArrowLeft className="h-4 w-4" />
        Back to feeds
      </Link>
      <h1 className="text-2xl font-bold mb-6">Add New Feed</h1>
      <FeedForm />
    </main>
  )
}
```

### 2. FeedFormの登録成功後リダイレクト対応 🔵

**信頼性**: 🔵 *user-stories.md 1.1 登録後フロー・Next.js routerより*

`components/feed-form.tsx` に `onSuccess` コールバックプロップを追加（または既存実装を調整）:
- 登録成功後: `router.push('/')` でトップページへリダイレクト
- またはFeedFormに `redirectTo` プロップを追加

---

## 単体テスト要件

### テストケース1: ページの基本表示 🔵

**信頼性**: 🔵 *architecture.md ページ設計より*

**テストファイル**: `app/feeds/new/page.test.tsx`

**Given**: NewFeedPageコンポーネントをレンダリング
**When**: 初期表示
**Then**: 「Add New Feed」タイトル、「Back to feeds」リンク、FeedFormが表示される

---

## UI/UX要件

### レイアウト 🟡

**信頼性**: 🟡 *一般的なフォームページレイアウトから推測*

- `max-w-2xl` で適切な幅に制限
- 「Back to feeds」戻りリンクをページ上部に配置

---

## 実装手順

1. `/tsumiki:tdd-requirements TASK-0012` - 詳細要件定義
2. `/tsumiki:tdd-testcases` - テストケース作成
3. `/tsumiki:tdd-red` - テスト実装（失敗）
4. `/tsumiki:tdd-green` - 最小実装
5. `/tsumiki:tdd-refactor` - リファクタリング
6. `/tsumiki:tdd-verify-complete` - 品質確認

---

## 信頼性レベルサマリー

| カテゴリ | 🔵 青 | 🟡 黄 | 🔴 赤 | 合計 |
|---------|-------|-------|-------|------|
| 実装詳細 | 2 | 0 | 0 | 2 |
| 単体テスト | 1 | 0 | 0 | 1 |
| UI/UX要件 | 0 | 1 | 0 | 1 |

- **総項目数**: 4項目
- 🔵 **青信号**: 3項目 (75%)
- 🟡 **黄信号**: 1項目 (25%)
- **品質評価**: 高品質
